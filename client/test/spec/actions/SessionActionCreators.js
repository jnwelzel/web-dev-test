'use strict';

describe('SessionActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/SessionActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
